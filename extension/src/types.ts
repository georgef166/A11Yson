export interface ArticleData {
    title: string;
    content: string;
    textContent: string;
}

export interface OverlaySettings {
    fontSize: number;
    fontFamily?: string;
    hideImages: boolean;
    lineHeight?: number;
}
